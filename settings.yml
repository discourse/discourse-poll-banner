topic_id:
  default: 0
  type: integer
  description: "Poll must be in the first post of this topic"

poll_name:
  default: example
  description: "When creating a poll you must add a name to the poll's bbcode; [name=example]"

poll_key:
  default: "0 (not likely) - 10 (very likely)"

show_after: 
  default: 20
  description: "Start showing the banner N minutes after someone's first visit"

stop_after: 
  default: 10080
  description: "Stop showing the banner if someone hasn't voted within N minutes"